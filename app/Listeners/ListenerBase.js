class ListenerBase {

    // tell app if you want it to go in queue stack and the name of queu
    queue = true
    queueName = "default"

    constructor(opts) {

    }
}

module.exports = ListenerBase